server:
  port: ${PORT:0}

spring:
  application:
    name: serie-service


message: hello from serie-service

management:
  endpoints:
    web:
      exposure:
        include: '*'

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka